# require "dry/cli"

module CLI
  module Commands
    # extend Dry::CLI::Registry

    # register "version", Container['cli.commands.version'], aliases: ["v", "-v", "--version"]
  end
end

# Dry::CLI.new(Foo::CLI::Commands).call
